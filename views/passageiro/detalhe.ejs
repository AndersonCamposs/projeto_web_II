<!DOCTYPE html>
<html lang="en">

<%- include('../partials/head', { title: "Detalhe de passageiro" }) %>

<body>
  <%- include('../partials/navBar') %>
  <div class="container mt-3">
    <% if (!passageiro) { %>
    <div class="alert alert-warning text-center" role="alert">
      Passageiro não encontrado. Para listar todos os passageiros, <a href="/passageiros" class="alert-link">clique aqui</a>.
    </div>
    <% } else { %>
    <p class="text-center fs-3 my-3">Informações do passageiro</p>
    <div class="card mb-3">
      <div class="card-header">
        <i class="fa-solid fa-circle-info"></i> Informações pessoais
      </div>
      <div class="card-body">
        <div class="row">
          <div class="col-12 col-sm-12 col-md-12 col-lg-6">
            <div class="input-group mb-3">
              <button class="btn btn-secondary" style="width: 100px;" type="button" id="button-addon1" data-bs-toggle="tooltip" title="Nome do passageiro">Nome</button>
              <input type="text" class="form-control" value="<%= passageiro.nome%>" aria-describedby="button-addon1" disabled>
            </div>
          </div>
          <div class="col-12 col-sm-12 col-md-6 col-lg-3">
            <div class="input-group mb-3">
              <button class="btn btn-secondary" style="width: 100px;" type="button" id="button-addon1" data-bs-toggle="tooltip" title="CPF do passageiro">CPF</button>
              <input type="text" class="form-control" value="<%= passageiro.cpf%>" aria-describedby="button-addon1" disabled>
            </div>
          </div>
          <div class="col-12 col-sm-12 col-md-6 col-lg-3">
            <div class="input-group mb-3">
              <button class="btn btn-secondary" style="width: 100px;" type="button" id="button-addon1" data-bs-toggle="tooltip" title="RG do passageiro">RG</button>
              <input type="text" class="form-control" value="<%= passageiro.rg%>" aria-describedby="button-addon1" disabled>
            </div>
          </div>
        </div>
      </div>
    </div>
    <div class="card mb-3">
      <div class="card-header">
        <i class="fa-solid fa-house-user me-1"></i> Informações do endereço
      </div>
      <div class="card-body">
        Informações
      </div>
    </div>
    <% } %>
  </div>
</body>

</html>